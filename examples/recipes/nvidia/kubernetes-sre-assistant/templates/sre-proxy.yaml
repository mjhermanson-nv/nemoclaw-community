# SPDX-FileCopyrightText: Copyright (c) 2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved.
# SPDX-License-Identifier: Apache-2.0
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "sre-assistant.proxyName" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "sre-assistant.labels" . | nindent 4 }}
    app.kubernetes.io/component: sre-proxy
spec:
  replicas: 1
  selector:
    matchLabels:
      {{- include "sre-assistant.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: sre-proxy
  template:
    metadata:
      annotations:
        checksum/api-proxy: {{ .Files.Get "files/scripts/api_proxy.py" | sha256sum }}
      labels:
        {{- include "sre-assistant.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: sre-proxy
    spec:
      serviceAccountName: {{ include "sre-assistant.proxyName" . }}
      automountServiceAccountToken: true
      securityContext:
        seccompProfile:
          type: RuntimeDefault
        {{- if not .Values.deployer.platform.openshift.enabled }}
        runAsUser: 65532
        runAsGroup: 65532
        fsGroup: 65532
        {{- end }}
      {{- with .Values.deployer.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: proxy
          image: {{ .Values.global.sre.utilityImage | quote }}
          imagePullPolicy: IfNotPresent
          command: ["/usr/local/bin/python3", "-B", "/runtime/api_proxy.py"]
          env:
            - name: PROXY_PORT
              value: {{ .Values.global.sre.proxy.port | quote }}
            - name: PROXY_ALLOWED_METHODS
              value: {{ ternary "GET,POST,PUT,PATCH" "GET,PATCH" (eq .Values.global.sre.rbac.mode "broad-no-delete") | quote }}
            - name: PROXY_POLICY
              value: general
            {{- if .Values.global.sre.openshiftLlmDeploy.enabled }}
            - name: PROXY_SERVICE_PROXY_NAMESPACE
              value: {{ include "sre-assistant.modelDeployNamespace" . | quote }}
            {{- end }}
            - name: PROXY_CLIENT_TOKEN_FILE
              value: /proxy-auth/token
            - name: PROXY_TLS_CERT_FILE
              value: /proxy-tls/tls.crt
            - name: PROXY_TLS_KEY_FILE
              value: /proxy-tls/tls.key
            - name: PROXY_MAX_REQUEST_BYTES
              value: {{ printf "%d" (int64 .Values.global.sre.proxy.maxRequestBytes) | quote }}
            - name: PROXY_MAX_RESPONSE_BYTES
              value: {{ printf "%d" (int64 .Values.global.sre.proxy.maxResponseBytes) | quote }}
            - name: PROXY_REQUEST_TIMEOUT_SECONDS
              value: {{ .Values.global.sre.proxy.requestTimeoutSeconds | quote }}
          ports:
            - name: https
              containerPort: {{ .Values.global.sre.proxy.port }}
              protocol: TCP
          securityContext:
            runAsNonRoot: true
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
          resources:
            requests:
              cpu: 10m
              memory: 32Mi
            limits:
              cpu: 250m
              memory: 128Mi
          volumeMounts:
            - name: runtime
              mountPath: /runtime
              readOnly: true
            - name: proxy-auth
              mountPath: /proxy-auth
              readOnly: true
            - name: proxy-tls
              mountPath: /proxy-tls
              readOnly: true
      volumes:
        - name: runtime
          configMap:
            name: {{ include "sre-assistant.runtimeConfigMapName" . }}
            defaultMode: 0444
            items:
              - key: api_proxy.py
                path: api_proxy.py
        - name: proxy-auth
          secret:
            secretName: {{ include "sre-assistant.proxyAuthSecretName" . }}
            defaultMode: 0440
            items:
              - key: {{ .Values.global.sre.proxy.authSecretRef.key | quote }}
                path: token
        - name: proxy-tls
          secret:
            secretName: {{ include "sre-assistant.proxyTlsSecretName" . }}
            defaultMode: 0440
            items:
              - key: {{ .Values.global.sre.proxy.tlsSecretRef.certKey | quote }}
                path: tls.crt
              - key: {{ .Values.global.sre.proxy.tlsSecretRef.privateKeyKey | quote }}
                path: tls.key
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "sre-assistant.proxyName" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "sre-assistant.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    {{- include "sre-assistant.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: sre-proxy
  ports:
    - name: https
      port: {{ .Values.global.sre.proxy.port }}
      targetPort: https
      protocol: TCP
